# cargo-features = ["codegen-backend"]
[workspace]
resolver = "2"
members = [
  "constraint_schema",
  "molecule_schema",
  # "generate_schema",
  "serde_types",
  "output_types",
]


# [profile.dev]
# debug = 0
# codegen-backend = "cranelift"
